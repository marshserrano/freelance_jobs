<% provide(:title, "Dashboard") %>

<%= render 'index_sidebar' %>

<div class="row">
  <% if current_user.employer? %>
    <div class="col-md-8">
      <section class="user_info"> <br>
        <h4>Job Posts (<%= @job_posts.count %>)</h4>
        <%= link_to 'Add New', job_posts_new_path, class:"btn btn-default btn-sm" %>
        <br><br>
        <% if @job_posts.any? %>
          <ul class="job_posts">
            <%= render @job_posts %>
          </ul>
        <% end %>
      </section><br><br>
    </div>
  <% end %>
</div>
